services:
  qdrant:
    image: qdrant/qdrant:latest
    ports:
      - "6433:6333"   # REST API
      - "6434:6334"   # gRPC
    volumes:
      - qdrant_data:/qdrant/storage
    restart: unless-stopped

volumes:
  qdrant_data:
