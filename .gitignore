log/
faiss*
secret.py
.DS_Store
__pycache__/
eval*/
scholargym_paper_db.json

# Large data files
*.pkl
*.pt
*.bin
*.safetensors
*.claude

# Paper directory
paper/
data/vector_db_checkpoint.json
Total Plan.txt

# Claude local project settings
.claude/settings.local.json

# Local experiment artifacts
logs/
runs/
paper_metrics/
data/fast_benchmark_data/
docs/
{alt})*
{path})*
experiments.yaml

# Ignore all text artifacts
*.txt

# iter25 experiment outputs (mirrors existing 'runs/' rule)
runs_iter25/

# Manual backups
*.bak
*.pre-think-bak
*.bak-pre-merge

# Private working notes
conversation_export.md
experiment_plan.md

# Local-only experiment manifest (env vars, API keys baked in)
experiments_iter25.yaml

# Local-only benchmark variant
data/superlong_bench_300_resume.jsonl

# One-off probe / utility scripts (kept locally, not part of pipeline)
scripts/check_*.py
scripts/survey_*.py
scripts/time_*.py
scripts/verify_*.py
scripts/exp/rename_runs.py
